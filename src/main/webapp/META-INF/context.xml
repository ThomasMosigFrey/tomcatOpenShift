<Context>
    <Resource name="jdbc/sendungsdaten" auth="Container" type="javax.sql.DataSource"
          username="${dbuser}"
          password="${dbpassword}"
          driverClassName="org.postgresql.Driver"
          url="jdbc:postgresql://<${dbhost}>:${dbport}/${dbname}"
          maxActive="20"
          maxIdle="10"
          validationQuery="select 1" />
</Context>