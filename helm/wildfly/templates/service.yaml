apiVersion: v1
kind: Service
metadata:
  name: {{ include "wildfly.fullname" . }}
  labels:
    {{- include "wildfly.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "wildfly.selectorLabels" . | nindent 4 }}
